#Explicación de utilidades de Flexbox:

#Las utilidades de Flexbox fueron esenciales para la creación de este layout responsivo y modular. 
#Utilice **`flex`** y **`flex-col`** para establecer la dirección y la capacidad de crecimiento en el Dashboard. 
#La clase **`flex-1`** se aplicó al contenido principal para que ocupara el espacio restante, forzando al **`<footer>`** 
#al final de la pantalla (patrón Sticky Footer). En el **`header`**, **`justify-between`** separó el título de las acciones, 
#y **`items-center`** se usó para el centrado vertical. 
#El menú desplegable del perfil, aunque ahora en el Header, sigue usando estas utilidades Flexbox para la organización interna.